import { TerminalPromptDirective } from './terminal-prompt.directive';

describe('TerminalPromptDirective', () => {
  it('should create an instance', () => {
    const directive = new TerminalPromptDirective();
    expect(directive).toBeTruthy();
  });
});
